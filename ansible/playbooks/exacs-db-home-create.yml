---
- name: ExaCS Database Home Create
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - "{{ operation_file }}"
  vars:
    exacs_auth_type: "{{ lookup('ansible.builtin.env', 'OCI_ANSIBLE_AUTH_TYPE') | default('instance_principal', true) }}"
    exacs_resource_tags:
      exacs_project: "{{ exacs_project }}"
      exacs_environment: "{{ exacs_environment }}"
      exacs_resource_key: "{{ exacs_db_home_key }}"
  pre_tasks:
    - name: Require an explicit execution mode
      ansible.builtin.import_tasks: common/exacs/operation/require-execution-mode.yml
  tasks:
    - name: Precheck the Database Home creation
      ansible.builtin.import_tasks: common/exacs/db-home-create/precheck.yml

    - name: Apply the approved Database Home creation
      ansible.builtin.import_tasks: common/exacs/db-home-create/apply.yml
      when: operation_execution_mode == 'execute'

    - name: Verify the Database Home creation
      ansible.builtin.import_tasks: common/exacs/db-home-create/verify.yml
      when: operation_execution_mode == 'execute'
